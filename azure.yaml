# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: lenderboxd
services:  
  app:
    language: dotnet
    project: ./Lenderboxd.AppHost/Lenderboxd.AppHost.csproj
    host: containerapp
